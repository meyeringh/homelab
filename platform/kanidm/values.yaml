app-template:
  controllers:
    kanidm:
      type: statefulset
      containers:
        app:
          image:
            repository: docker.io/kanidm/server
            tag: 1.11.1
          # Kanidm serves TLS directly; kubelet does not verify the self-signed cert.
          # Without these the process can wedge on blocked storage I/O while the pod
          # still reports Running, which hides the outage indefinitely.
          probes:
            liveness:
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /status
                  port: 443
                  scheme: HTTPS
                periodSeconds: 10
                timeoutSeconds: 5
                failureThreshold: 6
            readiness:
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /status
                  port: 443
                  scheme: HTTPS
                periodSeconds: 10
                timeoutSeconds: 5
                failureThreshold: 3
            startup:
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /status
                  port: 443
                  scheme: HTTPS
                periodSeconds: 10
                failureThreshold: 30
        # TODO remove when Cilium supports BackendTLSPolicy (upstream TLS)
        tls-proxy:
          image:
            repository: docker.io/nginx
            tag: alpine
          # Readiness only: proves the gateway-facing chain works end to end. Restarting
          # nginx would not fix a wedged upstream, so no liveness probe here.
          probes:
            liveness:
              enabled: false
            readiness:
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /status
                  port: 8080
                periodSeconds: 10
                timeoutSeconds: 5
                failureThreshold: 3
            startup:
              enabled: false
      statefulset:
        volumeClaimTemplates:
          - name: data
            size: 1Gi
            globalMounts:
              - path: /data
            accessMode: "ReadWriteOnce"
  configMaps:
    config:
      enabled: true
      data:
        server.toml: |
          bindaddress = "[::]:443"
          ldapbindaddress = "[::]:636"
          trust_x_forward_for = true
          db_path = "/data/kanidm.db"
          tls_chain = "/data/ca.crt"
          tls_key = "/data/tls.key"
          domain = "auth.meyeringh.org"
          origin = "https://auth.meyeringh.org"
    nginx:
      enabled: true
      data:
        default.conf: |
          server {
              listen 8080;
              location / {
                  proxy_pass https://127.0.0.1:443;
                  proxy_ssl_verify off;
                  proxy_set_header Host $host;
                  proxy_set_header X-Real-IP $remote_addr;
                  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                  proxy_set_header X-Forwarded-Proto $scheme;
              }
          }
  service:
    app:
      controller: kanidm
      ports:
        http:
          port: 8080
          targetPort: 8080
        https:
          port: 443
        ldap:
          port: 636
  ingress:
    main:
      enabled: false

  route:
    main:
      parentRefs:
        - name: main
          namespace: gateway
          sectionName: https
      hostnames:
        - auth.meyeringh.org
      rules:
        - backendRefs:
            - identifier: app
              port: 8080
  persistence:
    config:
      enabled: true
      type: configMap
      identifier: config
      advancedMounts:
        kanidm:
          app:
            - path: /data/server.toml
              subPath: server.toml
    nginx-config:
      enabled: true
      type: configMap
      identifier: nginx
      advancedMounts:
        kanidm:
          tls-proxy:
            - path: /etc/nginx/conf.d/default.conf
              subPath: default.conf
    tls:
      enabled: true
      type: secret
      name: kanidm-selfsigned-certificate
      advancedMounts:
        kanidm:
          app:
            - path: /data/ca.crt
              subPath: ca.crt
            - path: /data/tls.key
              subPath: tls.key
