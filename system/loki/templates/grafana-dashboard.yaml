apiVersion: v1
kind: ConfigMap
metadata:
  name: logs-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: Logs
data:
  logs-overview.json: |
{{ .Files.Get "files/logs-overview.json" | indent 4 }}
