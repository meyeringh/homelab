apiVersion: v1
kind: ConfigMap
metadata:
  name: volsync-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: Backups
data:
  volsync-dashboard.json: |
{{ .Files.Get "files/volsync-dashboard.json" | indent 4 }}
