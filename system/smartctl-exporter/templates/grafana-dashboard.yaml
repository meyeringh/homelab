apiVersion: v1
kind: ConfigMap
metadata:
  name: smart-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: Storage
data:
  smart-health.json: |
{{ .Files.Get "files/smart-health.json" | indent 4 }}
