apiVersion: v1
kind: ConfigMap
metadata:
  name: energy-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: Home
data:
  energy-dashboard.json: |
{{ .Files.Get "files/energy-dashboard.json" | indent 4 }}
